<!DOCTYPE html>
<html>
  <head>
    <title>Graduation Development</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application" %>
    <%= favicon_link_tag 'favicon.png' %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <%= render 'shared/header' %>
    <% if flash.any? %>
      <div class="container mx-auto px-4 mt-4">
        <% flash.each do |type, message| %>
          <% next if message.blank? %>
          <div class="mb-3 rounded-md px-4 py-3 text-sm <%= type.to_s == 'notice' ? 'bg-green-50 text-green-800 border border-green-200' : 'bg-red-50 text-red-800 border border-red-200' %>">
            <%= message %>
          </div>
        <% end %>
      </div>
    <% end %>
    <main style="width: 100%; margin-top: 7rem;">
      <%= yield %>
    </main>
    <%= render 'shared/footer' %>
  </body>
</html>
