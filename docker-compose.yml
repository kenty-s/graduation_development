services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: graduation_development
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  web:
    build:
      context: .
      dockerfile: Dockerfile.dev # 開発用 Dockerfile
    environment:
      RAILS_ENV: development
    volumes:
      - .:/app
      - bundle_data:/app/vendor/bundle
    ports:
      - "3000:3000"
    depends_on:
      - db
    command: >
      bash -c "bundle check || bundle install && bin/rails server -b 0.0.0.0"

  web_prod:
    build:
      context: .
      dockerfile: Dockerfile # 本番用 Dockerfile
    env_file:
      - .env   # ← これを追加！
    environment:
      RAILS_ENV: production
    ports:
      - "80:80"
    depends_on:
      - db

volumes:
  db_data:
  bundle_data:
